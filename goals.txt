Goals In Priority:

1. Implement info pages
2. Hover and select of history list item is offset wrong and fix scrollbar in info page
2. Containerize project and make sure can run on any machine
3. Think about maybe also exposing how many prev weeks you want to extract and how many weeks to predict for future
4. Expose the metadata in the model outputs page in like a side bar for users to see
5. Reset filter button and also each page should have memory of its last used filters

Long Term Goals:

1. UI rework of the filters and add tooltips for buttons
1. Add some sort of agent that either talks to an LLM api or uses Langchain or Hugging Face
2. Add authorization, probably JWT which oath and openID so that users can sign in with other websites like google, etc
3. See is it is viable to connect to sleeper leagues and espn leagues so that we can extract relevant data for a user